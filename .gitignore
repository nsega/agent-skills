# OS cruft
.DS_Store

# Local Claude Code settings (per-machine permissions)
.claude/settings.local.json

# Per-skill scratch / iteration workspaces (not real skills)
*-workspace/

# cross-review outputs (default to /tmp, but never commit them if redirected here)
*.findings.json
cross-review-packet.*
